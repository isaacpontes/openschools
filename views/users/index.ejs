<%- include('../layouts/header') %>

<section class="section has-background-light">
  <div class="container">
    <div class="card">
      <div class="card-content">
        <div class="columns">
          <div class="column is-four-fifths">
            <h1 class="title size-4">Listagem de Usuários</h1>
          </div>
          <div class="column is-one-fifth">
            <a href="/users/new" class="button is-link">Novo Usuário</a>
          </div>
        </div>
      </div>

      <div class="card-content">
        <%- include('../partials/messages') %>
        <table class="table">
          <thead>
            <tr>
              <th>Nome</th>
              <th>Email</th>
              <th colspan="3">Ações</th>
            </tr>
          </thead>
          <tbody>
            <% users.forEach(user => { %>
              <tr>
                <td>
                  <%= user.name %>
                </td>
                <td>
                  <%= user.email %>
                </td>
                <td>
                  <a href="/users/<%= user._id %>">Exibir</a>
                </td>
                <td>
                  <a href="/users/<%= user._id %>/edit">Editar</a>
                </td>
                <td>
                  <a href="/users/<%= user._id %>?_method=delete" class="has-text-danger">Excluir</a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>

<%- include('../layouts/footer') %>
